import { Box } from "@mui/system";
import TabCategory from "../components/TabCategory";

const Portal = () => {
  return (
    <Box sx={{ marginTop: 10 }}>
      <p>ini page portal</p>
      <TabCategory />
    </Box>
  );
};

export default Portal;
